import React from "react";
import { motion } from "motion/react";

const Step3 = () => {
  return <body>

    <p>Step 3</p>
    <p>Upgrade pip</p>

    <p> When you downloaded python pip was downloaded aswell but this will need to be upgraded to the latest version. Pip is a package mangager for python which means that you are able to download libraries using the terminal. </p>

    <p>Open your terminal of choise. This tutorial will use the command prompt. Run the command python -m pip install -U pip </p>


    <p>Now that pip is upgraded you can install OpenAIs libraries</p>
    <p>Run the command pip install openai to download the library</p>
    <img></img>
  
  </body>;
};

export default Step3;
